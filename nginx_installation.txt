Dependencies:
    * gcc
    * g++
    * make
    * pcre-8.41
    * zlib-1.2.11

$ sudo apt-get install gcc
$ sudo apt-get install g++
$ sudo apt-get install make
$ cd nginx-1.14.0
$ ./configure --with-pcre=<path/to/pcre/source/code> --with-zlib=<path/to/zlib/source/code>
$ make